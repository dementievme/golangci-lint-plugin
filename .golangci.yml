linters-settings:
  custom:
    loglinter:
      path: ./loglinter.so
      description: "Checks log messages for style and security violations"
      original-url: "https://github.com/dementievme/golangci-lint-plugin"

linters:
  enable:
    - loglinter
    - govet

run:
  timeout: 5m